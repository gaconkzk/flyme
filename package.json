{
  "name": "flymfe",
  "version": "1.0.0",
  "description": "Me MFE big prj",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-core": "pnpm run dev --filter=./packages/core",
    "dev-app": "pnpm run dev --filter=./packages/app",
    "dev": "concurrently \"pnpm run dev-core\" \"pnpm run dev-app\""
  },
  "author": "gaconkzk",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
